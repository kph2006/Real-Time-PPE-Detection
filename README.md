# Real-Time-PPE-Detection
This is a real-time AI-based python code which uses an Arduino IDE code to detect helmets ,masks and to check if a worker has consumed alcohol or not. If the worker is wearing both the helmet and the mask and has no alcohol consumption then the gate opens (through a servo motor) otherwise access is denied and the gate remains closed.
